branch-defaults:
  master:
    environment: atyourservice-env
global:
  application_name: AtYourService
  default_platform: Node.js
  default_ec2_keyname: ays-env
  default_region: us-east-1
  profile: eb-cli
  sc: git
deploy:
  artifact: dist.zip
